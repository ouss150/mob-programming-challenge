{{!-- le premier this représente images (si tu envoies le param dans home) --}}
<div class="gallery">
    {{#each this}} 
    {{!-- le second this représente chaque valeur de l'array images --}}
    <img src="/assets/{{this}}" alt="image">
    {{/each}}
</div>